# These properties are located by WebContextUtil to locate
# the servlet container during functional testing.
context.scheme=http
context.host=localhost
context.port=${jetty.connectorPort}
context.path=${ft.context.path}