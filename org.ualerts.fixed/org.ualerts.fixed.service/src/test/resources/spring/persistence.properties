persistence.unit=${persistence.unit}
hibernate.dialect=${it.hibernate.dialect}
hibernate.connection.driver_class = ${it.dataSource.driverClassName}
hibernate.connection.url = ${it.dataSource.url}
hibernate.connection.username = ${it.dataSource.username}
hibernate.connection.password = ${it.dataSource.password}
hibernate.hbm2ddl.auto=${it.hibernate.schema}
hibernate.show_sql=false
hibernate.format_sql=false